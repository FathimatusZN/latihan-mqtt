# ============================================================
#  FILE    : docker/docker-compose.yml
#  FUNGSI  : Orkestrasi service — Industrial IoT Monitor v2
#  CARA    : sudo docker-compose up -d --build
#  SERVICE :
#    1. mosquitto  → MQTT broker
#    2. postgres   → Database PostgreSQL
#    3. backend    → Python FastAPI + MQTT subscriber
#    4. frontend   → Nginx serve static files
# ============================================================

version: "3.9"

services:

  # ── 0. MOSQUITTO MQTT BROKER ─────────────────────────────
  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: mqtt-broker
    restart: unless-stopped
    ports:
      - "1883:1883"   # TCP — untuk MQTTX desktop / paho-mqtt
      - "9001:9001"   # WebSocket — untuk browser / MQTTX Web
    volumes:
      - ../docker/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    networks:
      - mqtt-net
    healthcheck:
      test: ["CMD-SHELL", "mosquitto_pub -h localhost -t '$$SYS/test' -m '' -q 0 2>/dev/null || exit 0"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── 1. POSTGRESQL DATABASE ───────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: mqtt-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       monitoring_pabrik
      POSTGRES_USER:     postgres
      POSTGRES_PASSWORD: password_docker    # GANTI di production!
    volumes:
      - ../database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"   # Host:5433 → Container:5432
    networks:
      - mqtt-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d monitoring_pabrik"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── 2. PYTHON BACKEND (FastAPI + MQTT Subscriber) ───────
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    container_name: mqtt-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      mosquitto:
        condition: service_healthy
    environment:
      DB_HOST:        postgres
      DB_PORT:        "5432"
      DB_NAME:        monitoring_pabrik
      DB_USER:        postgres
      DB_PASSWORD:    password_docker
      MQTT_BROKER:    mosquitto
      MQTT_PORT:      "1883"
      MQTT_CLIENT_ID: efortech-docker-001
      API_HOST:       0.0.0.0
      API_PORT:       "8000"
      # Tidak ada MQTT_TOPIC global di v2 — topics di-manage dari DB
    ports:
      - "8000:8000"
    networks:
      - mqtt-net
    volumes:
      - ../backend:/app   # hot-reload saat development

  # ── 3. NGINX FRONTEND ────────────────────────────────────
  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
    container_name: mqtt-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "8080:80"     # Dashboard: http://localhost:8080
    networks:
      - mqtt-net

# ── NETWORKS ─────────────────────────────────────────────
networks:
  mqtt-net:
    driver: bridge

# ── VOLUMES ──────────────────────────────────────────────
volumes:
  postgres_data:
    driver: local
  mosquitto_data:
  mosquitto_log: